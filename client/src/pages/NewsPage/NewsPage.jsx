import styles from "./NewsPage.module.css";
import { PageWrapper, SectionHeading } from "../../shared";

export const NewsPage = () => {
    return (
        <PageWrapper>
            <SectionHeading>{`Новости`}</SectionHeading>
        </PageWrapper>
    );
};
